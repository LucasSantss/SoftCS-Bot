{
  "name": "softcs-telegram-bot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node --env-file=.env dev-server.js",
    "dev:vercel": "vercel dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
